# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# AI SDKs — Anthropic direct API (always used as fallback)
anthropic>=0.40.0

# AWS / Bedrock path (used when AWS_ACCESS_KEY_ID is set in .env)
strands-agents==0.1.0
boto3==1.35.36
botocore==1.35.36

# Data Validation & Settings
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Environment Management
python-dotenv==1.0.1

# RSS Feed Parsing
feedparser>=6.0.11
beautifulsoup4>=4.12.0

# CORS & Security
python-multipart==0.0.12
starlette>=0.37.2,<0.42.0

# HTTP Client (for tools)
httpx==0.27.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Development
black==24.10.0
ruff==0.7.0
mypy==1.13.0
