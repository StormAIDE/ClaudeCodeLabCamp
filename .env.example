# Application Configuration
APP_NAME=ClaudeCode Lab Agent
APP_ENV=development
DEBUG=true

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Claude Model Configuration (Bedrock)
CLAUDE_MODEL_ID=eu.anthropic.claude-sonnet-4-5-20250929-v1:0

# AWS credentials should be configured via AWS CLI or terminal session
# Run: aws configure
# or export AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_SESSION_TOKEN

# Note: AWS credentials are NOT needed in .env since they're configured in your terminal session
